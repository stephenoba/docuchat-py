[project]
name = "docuchat-py"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.135.3",
    "sqlmodel>=0.0.38",
]

[dependency-groups]
dev = [
    "alembic==1.18.4",
    "fastapi==0.135.3",
    "python-decouple>=3.8",
    "sqlmodel==0.0.38",
]
